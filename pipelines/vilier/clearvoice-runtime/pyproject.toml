[project]
name = "vilier-clearvoice-runtime"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["clearvoice", "soundfile", "scipy", "librosa", "setuptools<81"]

[tool.uv]
package = false
